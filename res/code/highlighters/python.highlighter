@syntax bracketed
@line-comment #
@selective-comment ''' '''
@string-sides " '
@separators =,.;:-+*/()[]{}&|!
declarations -> class def
code_inclusion -> import
mods -> with
func_breaks -> return break continue pass
conditions -> if else elif for while do
operators -> yield in
exceptions -> try except finally raise assert
values -> true false