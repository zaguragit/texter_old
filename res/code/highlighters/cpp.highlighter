@syntax bracketed
@line-comment //
@selective-comment /* */
@hex-prefix 0x
@binary-prefix 0b
@string-sides " '
@separators =,.;:-+*/()[]{}&|!
declarations -> struct typedef enum class
code_inclusion -> #include #define
mods -> static const unsigned
func_breaks -> return continue break case default
conditions -> if else switch for while do
//operators ->
//exceptions ->
//values ->